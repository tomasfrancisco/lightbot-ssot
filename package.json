{
  "name": "lightbot-ssot",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "link-browser-storage": "cd browser-storage && yarn link && yarn install",
    "link-types": "cd types && yarn link && yarn install",
    "link-widget-configuration": "cd widget-configuration && yarn link && yarn install",
    "link-packages": "yarn link-browser-storage && yarn link-types && yarn link-widget-configuration",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.2.4"
  },
  "dependencies": {
    "js-cookie": "^2.2.0"
  }
}
