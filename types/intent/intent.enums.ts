export enum IntentEventsEnum {
  WELCOME = "LIGHTBOT_WELCOME",
}

// We use this to identify the corresponding type in intent.errors.ts
export enum IntentErrorIdentifierEnum {
  INTENT_WITH_DEPENDENTS = "INTENT_WITH_DEPENDENTS",
}
